(function attachReceiverTarget(global) {
  "use strict";

  // Deployment replaces this generated copy with the receiver's immutable
  // dashboard identity. The tracked default keeps source edits safe to build.
  global.MultiHubReceiverTarget = { id: "" };
}(globalThis));
